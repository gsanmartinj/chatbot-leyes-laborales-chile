# Interfaces web
streamlit>=1.36
gradio>=4.44

# Base vectorial y embeddings locales
chromadb>=0.5.5
sentence-transformers>=3.0

# Extracción de texto de PDF y Word
pypdf>=4.3
python-docx>=1.1

# Variables de entorno (.env)
python-dotenv>=1.0

# (Opcional) Redacción de respuestas con un modelo de lenguaje.
# Solo se usan si configuras el proveedor en el .env; si no, la app
# funciona sin estos paquetes.
openai>=1.40          # endpoints compatibles con OpenAI (DeepSeek vía NVIDIA)
google-genai>=1.0     # alternativa: Gemini
